{
  "name": "portfolio",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "web": "npm run dev --workspace=web"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.1.14",
    "turbo": "latest"
  }
}
